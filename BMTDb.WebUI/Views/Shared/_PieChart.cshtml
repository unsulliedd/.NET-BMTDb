<script src="https://www.gstatic.com/charts/loader.js"></script>

<script>

    // Load the Visualization API and the corechart package.
    google.charts.load('current', {'packages':['corechart']});

    // Set a callback to run when the Google Visualization API is loaded.
    google.charts.setOnLoadCallback(drawChart);

    // Callback that creates and populates a data table,
    // instantiates the pie chart, passes in the data and
    // draws it.
    function drawChart() 
    {

        // Create the data table.
        var data = new google.visualization.DataTable();
        data.addColumn('string', 'Topping');
        data.addColumn('number', 'Slices');
        data.addRows
        ([
            ['Movies',  @Model.Movies.Count],
            ['Persons', @Model.Persons.Count],
            ['Genres',  @Model.Genres.Count],
            ['Studios', @Model.ProductionCompanies.Count],
        ]);

        // Set chart options
            var options = 
            {
                'title': 'BMTDb Database',
                'width': 500,
                'height': 400,
                'backgroundColor': 'transparent',
                'pieSliceBorderColor': 'none',
                pieHole: 0.61,
                pieSliceTextStyle: { color: 'black' },
                titleTextStyle: {color: '#9bc5c3', fontSize: 18,},
                legend: {textStyle: {color: 'white', fontSize: 14}},
                slices: [{color: '#163172'}, {color: '#1E56A0'}, {color: '#47B5FF'}, {color: '#BBE1FA'}]
            };

        // Instantiate and draw our chart, passing in some options.
        var chart = new google.visualization.PieChart(document.getElementById('chart_div'));

        chart.draw(data, options);
    }

</script>

<div id="chart_div"></div>