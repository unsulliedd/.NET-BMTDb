@model List<Genre>

@foreach (var item in Model)
{
    <a asp-controller="Movie" 
    asp-action="Index" 
    asp-route-genre="@item.Name"
    class="genre-list @(ViewBag.SelectedGenre == item.Name?"active":"")">
        @item.Name</a>
}